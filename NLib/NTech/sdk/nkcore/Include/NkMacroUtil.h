//////////////////////////////////////////////////////////////////////////
// 매크로 유틸리티
// 박현우 hwpark@ndoors.net
//////////////////////////////////////////////////////////////////////////
#pragma once

//////////////////////////////////////////////////////////////////////////
// Byte offset into a class's data members (starting at 0).
// FIELD_OFFSET 과 같음
#define MEMBER_OFFSET( type, field )	int( (LONG)(LONG_PTR)&(((type *)0)->field) )

//////////////////////////////////////////////////////////////////////////
